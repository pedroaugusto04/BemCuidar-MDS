import { Routes } from "@angular/router";
import { RequestProviderComponent } from "./request-provider.component";

export const REQUEST_PROVIDER_ROUTES: Routes = [
  {
    path: "",
    component: RequestProviderComponent,
  },
];
