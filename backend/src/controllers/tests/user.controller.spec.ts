describe("User controller tests", () => {
  beforeEach(() => jest.clearAllMocks());

  it("teste", () => {
    const first = 3;
    const second = 4;
    expect(first + second).toEqual(first + second);
  });
});
